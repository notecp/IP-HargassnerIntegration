name: Hassfest

on:
  push:
  pull_request:
  workflow_dispatch:
  # schedule:
  #   - cron: "0 0 * * *"

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Hassfest Validation
    steps:
      - name: Check out the Repository
        uses: actions/checkout@v4

      - name: Hassfest Validator Workflow
        uses: home-assistant/actions/hassfest@master
